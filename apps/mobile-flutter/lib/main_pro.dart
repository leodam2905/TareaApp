import 'flavor.dart';
import 'main.dart';

// Entry point for the Tarea Pro (handyman) app. Same codebase as Tarea Home,
// but this flavor shows the Pro landing/auth and lands on the Pro dashboard.
void main() => bootstrap(Flavor.pro);
